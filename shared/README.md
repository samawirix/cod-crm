# Shared

This directory contains shared utilities, types, and configurations that can be used by both frontend and backend.

## Structure

- `types/` - Shared TypeScript types
- `utils/` - Shared utility functions
- `constants/` - Shared constants

## Usage

These shared resources help maintain consistency between frontend and backend implementations.
